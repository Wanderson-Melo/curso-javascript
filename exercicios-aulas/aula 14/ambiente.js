//Estrutura de repetição com teste lógico no início
//nesta estrutura a condicional é avaliada antes da execução do bloco
for(var count = 1; count <= 15; count++){
    console.log(`Passo ${count}`);
};

//nota: não é necessário declarar a variável chamando a função var, porém como sou iniciante e não sei quais são as consequências disso vou continuar com essa boa prática.

//nota: sempre utilizar modo de depuração em meus códigos.